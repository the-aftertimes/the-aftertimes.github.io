# The Aftertimes - launch checklist

## Open

- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **Watch the FIRST haiku edition, and decide whether prose retires.** The next cron is the first unsupervised haiku run. Check three things on the live page: the poem's typography on a phone (24rem measure, clamp(1.22rem,4.4vw,1.5rem)), that the picture is one object with nobody in it, and that the archive row reads sensibly with a two-word title instead of a headline. If it holds for a few days, delete `run_pipeline` and the prose-only halves of critic/write/revise - the duplicated ILLUSTRATE/RENDER/RECORD tail in `run_haiku_pipeline` exists ONLY to keep `form: prose` byte-identical as a reverse gear, and it should not outlive its usefulness. **Recommendation: give it a week, then retire prose.**
- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **`judge_floor` is meaningless for haiku and is still the prose default of 5.** Nothing reads it on the haiku path yet, so there is no weak-pool retry - which is defensible when the pool is 30-odd candidates rather than 4, but it means a uniformly weak day publishes its best anyway with nothing objecting. Collect a fortnight of `quality.judge_score` from real editions, then decide whether a floor is worth a second batch. Do NOT carry the 5 across; it was picked for a saturated metric that gave 37 of 65 drafts a perfect 1.0.
- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **The haiku record keeps every losing candidate, and nothing reads them yet.** `quality.candidates` now holds the ~30 that scanned and were not picked, which is the pool a taste loop could learn from and the thing the prose pipeline threw away for weeks. Pairs with the verdict backlog: marking a haiku good or bad is a second's work, where a 230-word dispatch was a minute's.

- [ ] **The 20-a-day cap is PER MODEL, which means the paper can have more than 20 calls a day for free.** Established 09/09/2026: the quota is `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, value 20. Nothing says one pipeline has to spend them all on one model. Putting ideate on one flash model, the drafts on another and judge/revise/depict on a third turns a hard 20 into 60, at zero cost and no quality risk worth naming - and it would have prevented most of what looked like transient API flakiness through late August and early September. **Do this even if the haiku pivot lands**, because it also buys trial headroom. `haikutrial.TRIAL_MODELS` already has a walk-the-list helper worth generalising into `gemini.py`.
- [ ] **The paper publishes on `gemini-3.6-flash` and there are newer models on the same key: `gemini-3.7-flash` and `gemini-3.8-flash`.** Noticed while enumerating models on 09/09. The haiku trials were served by 3.7-flash and the output was good, but that is not evidence about the prose pipeline. Worth one trial run on 3.7 against the current write prompt before switching. Options: leave it (zero risk, possibly leaving quality on the table); switch outright (untested on prose); or run one `trial.yml` batch on 3.7 and compare against the archive's measured prose stats first. **Recommendation and default if nothing is said: the trial batch**, because `settings.gemini.write_model` already exists for exactly this and the comparison is one command.

## THE HAIKU PIVOT, decided 07/09/2026

**STATUS 09/09/2026: LIVE. `form: haiku` is committed and the next cron files a poem.** Verified end to end by tests/test_haiku_pipeline.py (the real pipeline against a temp repo, model calls mocked, asserting the record, index.html, the permalink, the archive and the three-call budget) and by a live picture draw through depict_haiku + illustrate. `form: prose` still runs the original pipeline byte-identically and is the way back.

Proven on real output: one call, 40 candidates, **36 scanned 5-7-5, 0% pre-industrial, 6% bleak**, served by `gemini-3.7-flash`. Batches are committed under `data/trials/haiku/`. Best of them - "the cloud licence clerk / stamps the thunderstorm for noon / dry rain takes two weeks" - is better than anything in the forty published dispatches.

Two prompt findings worth not relearning: ending the prompt on the 5-7-5 rule took the scan rate from 7/24 to 36/40, moving one paragraph and changing no words (same lever as Ghostwriters - the model obeys what it reads LAST); and both register faults were subject SELECTION, not tone, so the fix was removing the freedom (no privation subjects at all, no pre-industrial props) rather than adding a rule asking for a lighter touch.

- [x] **DONE 09/09/2026 - wired up and live.** All five sub-steps shipped; the notes below are kept because they record WHY each is shaped as it is. `haiku.to_dispatch` already produces the exact dict that `render.py`, `archive.py`, `card.py` and `email_render.py` read, and `render.py` splits `body` on newlines into paragraphs, so those four files need NO changes to work - only typographic polish. What is left:
  1. `settings.form: haiku|prose`, defaulting to `prose` until a real edition has been seen. A daily publisher with no way back is how one bad day becomes a bad week.
  2. `run.py`: branch to a haiku path replacing IDEATE/SELECT/WRITE/CHOOSE/REVISE with two calls (generate a batch, judge it). **The branch cannot go where I first put it** - `qcfg` and `context` are not defined until after `>>> IDEATE`. The safe shape is a separate `run_haiku_pipeline()` that duplicates the ILLUSTRATE/RENDER/RECORD tail, leaving the prose path byte-identical; the duplication is worth it for a transitional period and can be merged once prose retires.
  3. `critic.py` triage: length, rhythm, tricolon and flat_rule are meaningless on 17 syllables. Machine phrases, legal register, US spelling, plainness and the era rules all still apply. `quality.hard_reject` and the whole `quality` block need rewriting around 5-7-5 instead of word counts.
  4. `render.py` typography: three lines want a poem's leading and a narrow measure, not three body paragraphs with 1rem gaps between them.
  5. Re-derive `judge_floor` from the new distribution - it is a provisional 5 carried over from a saturated prose metric.
- [x] **SETTLED 09/09/2026, do not re-pitch: the picture KEEPS the Dore style.** Recorded as closed because it is a decision, not pending work. Charlie: "why can't we stick with current picture style?" - and he was right. Measured, the fix is asking that style to draw LESS, not to draw differently: one object, no people, and the smooth-grey mush drops from 16.9% to 3.1% with the style untouched (woodcut managed 2.8%, so this is no worse, and it keeps forty published dispatches consistent). The live style's failure on that test scene was a crowd of thirty near-identical faces, which is the "ai sloppy" complaint exactly, and it disappears because a haiku is one object rather than a populated scene. `haikutrial.STYLES["dore-object"]` is the wording that worked.
- [x] **DONE 09/09/2026 - the caption is gone, and the real fault was something else.** A `_NO_PLATE` clause after the negative block fixed the lettering. But the first clean live draw then came back with SEVEN PEOPLE round the object, because `_STYLE` asks for "figures in a believable environment" and `_NEGATIVE` composes them - the brief was not ignored, it was outvoted by the two constants `_fit` never drops. Object-only now has its own style and negative. Final measured result: 10.1% smooth grey, 2.4% ink, 87.5% paper, no people, no lettering. ORIGINAL NOTE:  Both object-only variants came back captioned - "APPROVING WEATHER", "ASEK WEATHER" - because one object on bare paper is exactly what a nineteenth-century catalogue plate looks like, and those have captions. The no-lettering rule is already in `illustrate._NEGATIVE` and lost anyway. The existing 9% margin crop cannot help: this text is mid-frame. Needs solving BEFORE the pivot ships, or every edition publishes with a misspelt caption on it.

- [x] **RESOLVED 09/09/2026, and the guess in this item was WRONG.** The allowance is not a mystery and does not reset at UTC midnight: it is `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, value **20 calls per day PER MODEL**, quoted verbatim from an untruncated 429 body. Google's "Please retry in Ns" hint alongside it is a bucket-refill estimate and is meaningless for a daily cap - trusting it produced two wrong diagnoses. Fixed three ways: error bodies are no longer truncated, a PerDay quota fails fast with the real reason, and every haiku stage walks a model list so a spent model costs nothing. The haiku pipeline uses 3 calls a day against the prose pipeline's 8-13. ORIGINAL NOTE:  Four haikutrial runs on 08/09/2026 all refused - one 503 spike and three `"You exceeded your current quota"` at 23:56, 23:57 and 23:58 UTC. **When the allowance resets is NOT established and must not be guessed:** the obvious theory is UTC midnight, and the 06/09 evidence contradicts it, because that day was exhausted at 11:57 UTC and generating happily again by 18:05. Do not write a reset time into a comment or a message until someone has actually measured it - that is precisely how `illustrate.py` accumulated three wrong explanations of Cloudflare's 4006.
  What IS established: `daily.yml` fires four times a day (18:00, ~18:40, ~19:55, ~21:50 UTC) and the prose pipeline costs about 8 calls per generating run, so the paper alone can account for the allowance. **The pivot fixes this as a side effect** - haiku is 1 generate + 1 judge + 1 depict, roughly 3 calls a day against ~8, so trial headroom comes back once it ships. Until then, run haikutrial well BEFORE 18:00 UTC, and expect it to compete.
  Cheapest way to measure the reset, if it is ever worth it: the refused call costs nothing, so a tiny hourly probe committing a timestamped success/failure line would map the window in a day.

Charlie, after forty dispatches: "i feel like a lot of them just aren't funny, not futuristic enough, not funny enough. and just don't make sense", and then "no I think just move to haikus now." The paper KEEPS its masthead, dateline, locator plate, engraving and archive; the dispatch becomes seventeen syllables. Do not re-litigate this - it was decided against a straight recommendation to spend two weeks feeding the verdict loop first, and he chose the pivot.

Why it is likely to work where the prose did not, so a future session does not undo the reasoning: **the economics invert.** A 230-word dispatch is a Gemini call each, so the old pipeline could afford four candidates and picked best-of-four - which is nowhere near selective enough to be funny, and no prompt fixes that. A haiku is ~15 tokens, so ONE call returns two dozen. Selection becomes the mechanism rather than a formality. It also makes the failure cheap (a weak haiku costs three seconds, and ambiguity in seventeen syllables reads as intent) and it fixes the picture, because a haiku IS one concrete image and `depict` no longer has to invent a visual moment a 230-word story never had.

Shipped so far: `haiku.py` (prompt, parse, a measured syllable heuristic - 96.5% at word level over an 85-word hand-counted fixture, so line-level is lower and the sieve is over 24 candidates for exactly that reason), `haikutrial.py` + `haikutrial.yml` (evidence harness, touches nothing the site reads).

- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **Still to convert, in this order.** `render.py` (three lines, not paragraphs - the poem wants air and a much narrower measure), `archive.py` and the archive row shape, `card.py` (the share card can now carry the poem itself, which the 230-word version never could), `email_render.py`, and `run.py`'s stage wiring. Do NOT start these until the poems are signed off, or the plumbing gets built twice.
- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **`critic.py` needs triage, not deletion.** Length, rhythm, tricolon and flat_rule are meaningless on a haiku and must go. Machine phrases, legal register, US spellings, plainness, present-day props and present-day institutions all still apply and are the checks that were doing real work. `hard_reject` and the `quality` block in settings need rewriting around 5-7-5 rather than around word counts.
- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **The judge is now the whole product.** It picks 1 from ~20 survivors instead of 1 from 4, which is a completely different job: asked to rank twenty it has to discriminate, where scoring four in isolation let it return 1.0 for everything (37 of 65 drafts scored a perfect 1.0 - see the site-style note on saturated metrics). Re-derive `judge_floor` from the new distribution rather than carrying the old provisional 5 across.
- [ ] **Verdicts and trial marks are still the only taste signal, and are still empty.** 40 dispatches, 0 verdicts; 13 trials, 0 marked. The pivot does not change that - it makes it cheaper to act on, because marking a haiku takes a second. `python verdict.py <date> good|bad "why"`.
- [x] **MOOT 10/09/2026 - the haiku pivot was reverted after one edition** (`form: prose`, commit a22652c). The haiku path is dormant, not deleted, so this becomes live again only if the switch is flipped back - and it should not be re-pitched. Original item: **The picture style is being chosen from a rendered sheet**, six styles on one fixed scene with everything else held constant. The diagnosis behind the options is in `haikutrial.STYLES`: the live Dore style asks a four-step distilled model for dense cross-hatching plus faces, which it cannot resolve, so it returns grey mush and waxy faces - Charlie's "ai sloppy". Every candidate asks for LESS resolution, not better wording.
- [x] **MOOT 10/09/2026 - nothing is orphaned; the paper is back on prose** (`form: prose`, commit a22652c). `write.py`'s pools, `proposals.py`, `trial.py`, `reedit.py` and the back catalogue are all in their native form again. Original item: **Retire or repoint what the pivot orphans.** `write.py`'s exemplars, funny-lines and flat-lines pools, `proposals.py`, `trial.py`, `reedit.py` and the 40 published dispatches all assume prose. The back catalogue STAYS as it is - it is the paper's own history - but anything that re-edits or re-judges it needs to know it is looking at the old form.

- [ ] **Re-edit 2026-09-06's WORDS once the Gemini daily quota resets.** The picture was redrawn successfully on 06/09; the prose was not. Three reedit attempts ran, and the third died on `HTTP 429 "you exceeded your current quota"` - the DAILY free-tier limit, not the per-minute one, exhausted by those same attempts plus two depict calls. The first two attempts are the interesting ones: the editor twice reported it had fixed exactly what Charlie named ("the passive rule announcement and the lazy three-item escalation list"), and both rewrites were binned for `legal_register` because the obvious way to give a rule an enforcer is a bailiff. `revise.py` now shows the editor the banned vocabulary, so the next attempt has a real chance. Run: `gh workflow run reedit.yml -f date=2026-09-06 -f dry=false`. Note the standing forward-only caveat: the pipeline fixes shipped today only affect dispatches written from 06/09 onward.
- [ ] **Watch whether the `focus` slot pushes flux prompts over budget routinely.** The 06/09 redraw dropped `materials` AND the `anomaly` to fit - two detail slots, which is more than `_fit` has ever had to drop before. That is working as designed (the focus object is in `core` and never goes), but if it happens most days the brief is systematically too long and the answer is a shorter `_STYLE` or a tighter guide, not a bigger `MAX_PROMPT` - the 2048 wall is Cloudflare's and hitting it costs the picture entirely.
- [ ] **`judge_floor` is still unmeasured, and now two days have run with no judge at all.** 02/09 and 06/09 both published a single unopposed draft (`scores: [0.92]`, empty `judge_reason`). The 503 backoff and the collapsed-pool retry should stop that, and `quality.write_failures` now records it in the dispatch itself - check in a week that the field is present and zero. If it is not zero on a day nobody noticed, that is the answer to the "nobody is watching the daily job" item above.

- [x] **NEWSLETTER RETIRED 13/08/2026 - this is a web-only site now.** Charlie's call
      after the shared Brevo account was suspended on 10/08 under the **Terms of Use**
      category (not the recoverable "negative results" one). The most likely objection
      is this site's content: daily, automated, AI-generated fictional news. That is
      clearly-labelled satire and not a genuine violation, but it is exactly what an
      automated compliance classifier flags, and the risk does not go away by moving to
      another marketing platform - it just relocates. The site itself is unaffected and
      keeps publishing daily.
      **What changed:** `signup_form_url` emptied (render.py's `_signup` returns "" on a
      falsy URL, so the signup section disappears from the page with no code change);
      `newsletter.enabled: false`; the send step, its failed-send issue alarm, the
      `test_send`/`test_email` inputs and the `issues: write` permission all removed from
      `daily.yml`; `brevo-admin.yml` deleted. `send_email.py`, `email_render.py` and
      `brevo_sender.py` are **left in place but wired to nothing** - retired, not deleted,
      so this is reversible. `tests/test_send.py::test_hold_short_circuits_the_send` now
      forces `enabled: True` in its own settings stub, so it still exercises the hold
      logic instead of passing because the feature is off.
      **Blast radius was the real lesson:** both newsletters shared one Brevo account, so
      this site's content risk took One Story down with it. One Story is moving to its own
      provider (Resend) on its own account - never share a sending account across two
      projects with different content-risk profiles again.
      **If it is ever revived:** use infrastructure-level sending (Amazon SES or a
      self-hosted sender), not a marketing platform. SES has contact lists and
      subscription management with `{{amazonSESUnsubscribeUrl}}`, charges ~$0.10/1,000
      emails, and does not apply marketing-compliance content classification - but note
      the account starts in a sandbox that needs production access approved, and only ONE
      contact list is allowed per AWS account.
- [x] **RESOLVED 20/08/2026 - the figure cap moved from the FRAME to the SUBJECT.** Charlie confirmed the hard one-or-two cap when asked, then saw the first picture that broke it - a woman at a camera tripod with her family lined up behind her - and said "it actually looks fine here with multiple figures". So background figures are in: one or two people are the subject, drawn larger and sharper, and others may stand behind them. The contradiction is gone - `illustrate._NEGATIVE` no longer says "no background figures" while the brief asks for them. What stays banned is the thing the cap was actually protecting against: a picture with no focal subject, a dozen equal faces at equal weight, which is what genuinely turns to mush in an engraving. CLAUDE.md's house-style note said the opposite and has been corrected in place, since a stale settled-style note is worse than none. **If a future picture is called crowded, make the subject dominate - do not empty the background again.**
- [x] **MEASURED AND LEFT ALONE 22/08/2026 - the lettering leak is real but occasional, not daily.** Read six engravings by eye rather than guessing: 15/08 clean, 17/08 clean, 18/08 clean, 19/08 carried two invented signs ("Paat", "LINtE"), 19/08 after its redraw clean, 20/08 carried three legible ones ("FROME", "ARCEE", "Conbrvtr"), 21/08 had faint illegible scribble on a bag and a tube. So roughly one in three shows something and one in six shows something a reader would notice, against the TODO's own test of "if it is rare, leave it". **The no-text clause is present on BOTH prompt paths and sits at the tail where flux weights it hardest, so there is no cheap input-side fix left** - the instruction is there and is sometimes ignored. The only real remedy is an output check (OCR the finished JPEG, redraw over a threshold), which costs a Cloudflare call on every false positive and now competes with the 2048-character prompt budget for nothing a reader has complained about. Both leaks sat on scenes that invite signage - a gallery frontage, a station corridor - so if this is ever revisited, the cheaper lever is to keep signage-adjacent objects out of the `setting` slot rather than to police the output.
- [x] **DONE 23/08/2026 - 22/08's text was re-edited and all four faults are fixed.** Built `reedit.py` + `reedit.yml`, the prose sibling of reillustrate: re-runs the editor over a published dispatch, words only, picture untouched. Every superseded version is appended to `record["revisions"]` with the critique and a timestamp, because a dated paper that silently changes its own back numbers is doing something worse than publishing a flat joke. The workflow input defaults to `dry`. Result on 22/08: "voting wands" became "ballot cards"; the coach is now "heard yelling on the tape" rather than quoted as if interviewed; the successor is "a sewage operator who had once tumbled down an entire elevator shaft during a live debate" instead of someone who had never dropped a spoon; the headline went from 7 words describing the premise to "Secretly Graceful Mayor Dies"; and the kicker is now "his pallbearers have been instructed to drop his coffin at least twice on the way to the grave". **Nothing older has been touched, and that stays Charlie's call** - the six archaic back-catalogue pieces are still open above.
  **Three bugs the dry runs caught before they published, all in the DAILY path, not in reedit:** (1) the editor returned `"scene": "OBITUARIES"` - it read the picture-brief field as a section label, and an accepted revision would have drawn the next redraw from that word. `revise.py` now carries the draft's scene straight across. (2) The new judge tolerance treated every minor as tradeable, so a nine-word headline could be bought with a better joke; `revise_judge_never` now lists the minors that are house rules rather than taste. (3) The editor was being marked against a seven-word cap its own prompt never stated - the write prompt explains it at length and the editor never sees the write prompt. Same shape as (1): a stage asked to do a job without the constraint the original author had.
- [x] **CUT 26/08/2026 - the domain filter is gone.** Charlie delegated the call. The deciding measurement: **17 of the 22 chips returned exactly one dispatch** and the largest returned three, so it was not a filter, it was the article list again in pill form. It cost 457px above the first story at 360px, and the list it filtered is 28 rows ordered by distance from now. Bucketing the domains was the runner-up and was rejected because the model invents domains beyond the configured 20 (22 distinct in 28 dispatches), so any mapping needs hand-maintenance forever to solve a problem the deletion solves outright. **Nothing is lost:** the domain still prints on every row. `_domain_chips`, the chip CSS, the filter JS and the per-row `data-domain` are all removed, the page now ships no JavaScript beyond the analytics beacon, and a guard test fails if the markup returns. Reversible from git if he ever actually wants to filter.
- [ ] **Nobody is watching the daily job except Charlie.** Logged 26/08/2026 at the wrap, after six silent failures in a month: a dead newsletter for three days behind `continue-on-error`, two pictureless days (prompt over the 2048 cap, then an exhausted neuron allocation), a day chosen with no comedy judgement because the judge is skipped when only one draft survives, a revise gate that could not improve a clean draft at all, and a depict fallback that fires with no trace. **In every case the system knew** - the reason was in an annotation, an error body, an empty `judge_reason`, a discarded-revision log line. What is missing is anything that says it out loud. Two shapes worth considering, neither built: (a) record the degradations rather than only logging them, so `quality.judge_reason == ""` and `dispatch.image is None` and "revision discarded" become queryable facts across the archive instead of expiring CI output; (b) a weekly digest, or an issue opened on a degraded run, so the first person to know is not the reader. The 26/08 backup-cron fill-in is the first piece of this and covers exactly one case.
- [ ] **Repeated character names: the detector cannot prevent the SECOND use, and the extractor is too noisy to fix it cheaply.** Surfaced 26/08/2026 by the first `proposals.md`, which reported "Kaelen Voss" in three dispatches - against a write-prompt rule that bans reusing character names. Investigated and deliberately not fixed, because the obvious fix does not survive measurement.
  - **Why it happens:** `trends.repeated_names` uses `min_count: 3`, so a name is only flagged once it has ALREADY been used three times. It prevents the fourth, never the second. The only forward defence is a hand-written list in `write.py` naming five specific offenders ("Vance", "Elena", "Rostova", "Marcus", "Kovac"), which is the hand-maintained list that always lags.
  - **The tempting fix, and why it was rejected:** feed the recently-used names into the avoid block so the model cannot reuse one - constrain rather than instruct. Measured: an 8-dispatch window gives 26 names / 353 chars, 12 gives 39 / 532, 20 gives 66 / 912, all inside the 1200-char cap. But `trends._NAME` is `\b([A-Z][a-z]{2,})\s+([A-Z][a-z]{2,})`, which matches ANY two capitalised words: the 12-dispatch sample contains "Because Nayar", "Cracking Open", "London Safe", "Watch Charter" and "Ice Shaft". Feeding that list to the writer as do-not-reuse would ban ordinary phrases. Across the whole archive it finds 101 "names" of which only 9 are reused, and the noise includes institutions ("Orbit Mutual") and titles ("Chief Magistrate").
  - **What it actually needs first:** a more precise extractor - require a speech verb nearby ("said", "told"), or a known honorific, or presence in the dispatch's own quotes - and only then a blocklist. Precision matters far more for an instruction than for a trend report, where a human reads it and noise averages out at n>=3. Do not ship the blocklist against the current regex.
- [x] **DONE 26/08/2026 - the back catalogue is re-edited; the archive measures clean.** Ran `reedit.yml -f archaic=true` once the day's dispatch had filed and the quota gate opened on its own. All six were rewritten: 30/07, 31/07, 01/08, 03/08, 08/08, 14/08. **Verified independently of the run log:** 29 records checked, 0 still measuring as a plainness fault, and 7 superseded versions preserved in `revisions` across the archive. Sample: "Neighbor's 'Alpine Blizzard' Garden Package Dumps Snowdrifts Over Fence" (10 words, US spelling) became "Neighbour Dumps Alpine Snow Over My Fence"; "High Court Orders Street Greasing For Mobile Whale Liver" became "Living Whale Liver Demands Pork Fat". Nothing was lost - every original headline and body is still in its record with the critique that replaced it.
- [ ] **`run_date` in UTC: recommendation is to leave it, and the reason is the seam.** Re-examined 26/08/2026. The cron fires 20:13 UTC, so the edition a Sydney reader gets on the morning of the 27th is filed as the 26th - the JSON key, the `d/<date>.html` permalink and the image name all trail the masthead, which is correct because it formats `run_time` in Sydney. **Switching does not double-publish, it SKIPS a date:** the run after the switch would compute the Sydney date, which is one ahead of the last filed UTC one, so the archive gains a permanent one-day hole in its filename sequence rather than a duplicate. Weigh that against the benefit, which is that nobody sees the current fault - it is visible only as the newest archive row looking a day stale. If it is ever done, the clean version is a migration that renames the existing `d/` files and leaves redirects, not a forward-only flip.
- [ ] **26/08 published with no engraving, and should heal itself on the next run.** The manual dispatch at 21:39 UTC (see the dropped-cron note) found Cloudflare's daily allocation already spent, and three retries across the evening were all refused correctly - the code will not hammer an endpoint that has said no. `fill_missing_image` now looks back a bounded few days rather than only at its own date, so the 19:13 run should draw it. **If it is still pictureless after that, the allocation is being exhausted before 19:13 UTC and that is the token-permission item below becoming urgent rather than merely useful.** Note the reset is not clean UTC midnight: a redraw at 23:56 was refused, and on 25/08 one failed at 01:03 and succeeded at 03:05.
- [ ] **The token item is now URGENT, not merely useful - 4006 is confirmed genuine, and the spend is invisible.** 27/08/2026: the exhausted branch of `illustrate.py` was printing a canned sentence and discarding Cloudflare's response body, so three separate explanations were written and all three were wrong (shared with photocopy; clears at the UTC roll; spent by trial/reedit runs). It now prints the raw body, and the body settles it: `AiError: you have used up your daily free allocation of 10,000 neurons ... code 4006`. So this IS real quota exhaustion, not an entitlement or plan problem - that possibility is now closed.
  **Which makes the remaining fact the important one: the FIRST image call of that UTC day was already refused, with nothing in this repo having drawn.** `illustrate.py` is the ONLY Cloudflare consumer in the repo (pinned by `test_only_run_and_reillustrate_spend_the_image_budget`), and it draws one picture a day. Something OUTSIDE this repo is consuming 10,000 neurons/day on account `44cfa6...9432`. Staggering crons cannot fix that and neither can a second provider - Gemini returned 429 on the same run, so both tiers were dry at once. **Until Account Analytics: Read is added to the token, every diagnosis here is guesswork, which is exactly what produced the three wrong answers above.**

- [ ] **The Cloudflare allocation window is NOT the UTC day - it appears to roll near 03:00 UTC.** Four observations, all from refusals and successes rather than from documentation: a redraw failed at 01:03 UTC on 26/08, succeeded at 03:05 UTC on 26/08, failed at 23:56 UTC on 26/08, and failed again at **02:44 UTC on 27/08** - a fresh UTC day on which neither project had drawn anything yet (Aftertimes' cron is 19:13, Photocopy's runs about 21:00). A calendar-day reset cannot explain that last one; a window rolling around 03:00 UTC explains all four.
  **Why it matters:** 03:00 UTC is 13:00 AEST, so the daily dispatch at 19:13 UTC sits roughly SIXTEEN HOURS into the window. Everything else on the account draws first. That is the opposite of what you want for the one job whose output a reader actually sees, and it is a likely part of why the picture keeps being the thing that fails.
  **Do not act on this by moving the cron yet.** 19:13 was chosen deliberately on 27/08 to dodge the 20:00-21:00 UTC band GitHub drops jobs from, and trading a reliable schedule for a better quota slot needs the usage numbers first - which is the token-permission item below. Recorded here so the next session has the four datapoints instead of re-deriving them one refusal at a time.
- [ ] **BLOCKED ON A TOKEN SCOPE - and the cheap route to the answer has now been TRIED AND RULED OUT.** `CF_API_TOKEN` lacks Account Analytics read: the GraphQL query returns HTTP 200 with `{"message": "not authorized for that account", "code": "authz"}` on `aiInferenceAdaptiveGroups`. (`GET /accounts/{id}/ai/usage` returns 400 - that route does not exist, despite an earlier note here claiming it.)
  **Charlie's action, two minutes:** Cloudflare -> My Profile -> API Tokens -> **Workers AI** -> Edit -> Add more -> **Account | Account Analytics | Read** -> Continue to summary -> Save, then `gh workflow run cf-usage.yml -f days=10`. Editing permissions does not change the token value, so the secret keeps working.
  **Attempted in the browser twice on 26/08 and abandoned both times, without saving** - the token still reads `Account.Workers AI, Account.Workers AI`, Active, verified after backing out. The edit form gives each permission row an unlabelled combobox with the selected group in a hidden field; Chrome's screenshots of that page time out at 30s; the added row's selection did not stick; and on the second attempt the actions menu stopped appearing in the accessibility tree at all. Desktop screen access was requested as a second way to see the form and was denied. No way to confirm the two existing Workers AI rows survive a save, on the credential the daily engraving depends on.
  **What was ruled out by measuring the artefacts instead (26/08):** counting actual image files written per UTC day across both projects. **Photocopy is not the problem** - it draws exactly ONE image a day, every day, without exception. And the allocation is **not** a simple images-per-day cap: 23/08 fitted four images (3 Aftertimes + 1 Photocopy) while 26/08 exhausted after only two. So per-image cost varies, or a third consumer exists on the account that neither repo can see. That is precisely the question the analytics permission answers and the artefacts cannot, which is why this stays blocked rather than being closed by inference.
  **Useful side finding:** the `site-stats CI` token already carries `Account.Cloudflare Pages, Account.Account Analytics`, so the permission is available on the account rather than something that needs requesting. Its secret is not in this repo and copying one between projects is the wrong fix.
- [ ] **18/08/2026 read of "Nuns Block Pipe Repair Over Holy Typo" - three faults, all now fixed in config, none verified on live output.** Charlie: "little bit too absurd and shouldnt be first person... also image doesnt really match what it's about."
  - **First person: style deleted.** It ran on `firstperson`. Fourth style cut for the same reason after letter, advice and review. Note the process failure worth remembering: on 17/08 I had looked at exactly this style, judged it salvageable, and only fixed its lede. See CLAUDE.md.
  - **Too absurd: the prompt was explicitly asking for it.** It said follow the premise "to increasingly ridiculous but consistent conclusions". Now reads "increasingly SERIOUS - never increasingly silly", with a new ONE ABSURDITY PER DISPATCH rule. Watch this one for over-correction: the judge scored all three drafts 1.0 and called the kicker "exceptionally sharp", so the piece was working by every measure the pipeline has. If dispatches start reading flat, this is the change that did it.
  - **Image: `depict.py` picked the wrong person, and flux drew that correctly.** The brief was "a station fitter in a grease-stained orange work suit... hands resting on a patching clamp" - a real moment from a story whose entire subject is six nuns. Caused by "if the story involves many people, pick the single one doing something", with nothing saying it must be the person the headline is about. Fixed. (That note is now out of date: the one-or-two-figures rule was moved from the FRAME to the SUBJECT on 20/08 - see the resolved item above.)
  - **UPDATE 19-20/08:** no longer true that the picture had to stay wrong. `reillustrate.yml` was built for exactly this and the 19/08 picture was redrawn twice from it. The prose changes remain unverified on live output, which is what the trial-workflow item above is for.
- [ ] **READABILITY - Charlie 17/08/2026: "they're just hard to read, they don't read like a news article, they use really archaic words and it's very verbose."** Measured the whole archive before touching anything, and the complaint splits three ways:
  - **Verbose: already fixed, and he is reading the back catalogue.** The 31/07 and 06/08 passes worked. Late July ran 271-317 words at a 21-30 word mean sentence; this week runs ~205 words at a 12-16 word mean. The archive page he was looking at surfaces the OLD ones as prominently as the new, so the paper reads worse than it currently writes.
  - **Archaic: real, and concentrated in the same old dispatches.** Decode rate (share of body words outside the commonest ~28k English words) ran 5.4-7.5% on 30/07-03/08 - apothecary, cobblestone, lard, chrism, vespers, hermitage, gravedigger, unlacing - against a 1.5-4.4% median since 04/08. **Root cause: plainness was the ONE quality dimension with no measurement behind it.** Rhythm, length, the legal-register crutch and present-day props were all measured, therefore selected for across the three drafts; the write prompt's "prefer the short Anglo-Saxon word" had nothing enforcing it and lost every time it competed with "invent the era's own objects, materials, rituals and turns of phrase".
  - **Not a news article: real, and it was a contradiction in the config.** The prompt says "open on concrete news" while `styles.yaml` told the `feature` style to do "scene-setting". Feature duly opened 13/08 on "At dawn, eleven air-dock workers knelt on the cold basalt floor of Lock Four" and `discovery` opened 14/08 on an abstract survey finding.
  **Fixed this session:** `critic.check_plainness()` scores the decode rate against a frozen `config/common_words.txt` (28k words, generated by `tools/build_common_words.py`, no runtime dependency), graded minor at 5% and major at 7% from the observed distribution and deliberately NOT in `hard_reject`; it names the offending words in the detail line, which is the string `revise.py` feeds the model. The rate is now logged every run and stored in the record's `quality.prose` block, so drift is visible next time instead of needing re-measuring. The write prompt's plain-word rule was extended from the headline to the body with both banned registers spelled out (antique and clinical), plus a rule that the era's strangeness belongs in invented names built from ORDINARY words. A first-sentence-carries-the-news rule was added, and the `feature` / `firstperson` guidance no longer contradicts it. Validated against the archive: the check fires on exactly the five archaic dispatches and passes this week's.
  **Still open, and both need Charlie:**
  - **Unverified on live output.** The critic change is verified against 19 real dispatches; the PROMPT change is not, because `trial.py` needs a Gemini key and the key only exists in Actions. Watch the `% to decode` figure in the next few runs, or add a manual trial workflow.
  - **The back catalogue is the thing he is actually reading.** Five dispatches (30/07, 31/07, 01/08, 03/08, 08/08 plus 14/08) now measure as faults but are already published. Re-running them through write + revise would fix the archive's worst impression of the paper, at ~6 Gemini calls each, but it rewrites published history - his call whether that is honest for a dated newspaper or whether they stand.

- [ ] **Ongoing: story-quality tuning.** Keep flagging flat/bland dispatches - but note the levers CHANGED on 06/08/2026 and hand-editing prompts is no longer the first move.
  - **First move now: `python verdict.py <date> good|bad "why"`.** A good verdict promotes that premise into `config/exemplars.yaml`, which steers every future generation. Few-shot examples dominate model output far more than instructions, so this is the highest-leverage lever available and it is the ONLY one that needs Charlie. `python verdict.py` with no arguments lists dispatches awaiting a call.
  - Automatic now: three drafts are written and scored by `critic.py`, a judge picks the funniest, and a critique-rewrite pass runs but is published only if it measures no worse. `trends.py` feeds a capped "recently over-used" block into both prompts each day.
  - Still hand-editable if a specific rule is wrong: the ideate prompt, the write prompt, `config/styles.yaml`, `config/places.yaml`, `config/engines.yaml`, the seed premises, the fixer maps in `write.py`. There is no better model at zero cost (see the disabled Pro entry below).
  - **The falsifiable check (from the spec):** if after a month the mean draft score has not moved and Charlie's good/bad ratio has not shifted, the learning loop is not earning its complexity and should be cut back to the trend spotter alone. The `quality` block in each dispatch record is the evidence - the first one lands with tomorrow's cron.
  - 31/07/2026 pass addressed Charlie's four complaints (Australian-sounding places, clunky/unfunny headline, non-news "council complaint" shape, flat writing). Verified improvement on a live sample: "Atmospheric Bailiffs Tow Repossessed Hurricane Over Unpaid Debts", datelined Verdigris Promenade, Saturn Envelope - with a mid-piece competing-lien turn and a municipal-parking-fine kicker.
  - Watch for the NEXT failure modes: over-reliance on the legal/bureaucratic register (bailiffs, injunctions, liens are becoming a crutch), and quotes that all sound like the same deadpan official.
- [x] **DONE 06/08/2026 - archive timeline reworked** (commit `f16d27e`, verified live on aftertimes.charlietrenorden.com: both label tiers rendering, rail rows present). Charlie picked options A + C without A's era bands. Sqrt scale replaced log, which was the actual cause of the right-hand bunching (the nearest dispatch moved from 48% to 9%); colliding labels now drop to a second tier behind a leader line (2311 holds tier 0, 2356 drops to tier 1); the axis is pinned to the configured 4000-year ceiling so it stays stable as dispatches land; and the LIST is now the timeline, ordered by distance from now with a year rail, which removed the redundancy of printing every dateline twice. Zero label overlaps measured at a real 700px width. A `test_render` ordering test correctly failed and was rewritten to assert distance ordering.
  <details><summary>Original defect report</summary>
  Observed defects on the live page: adjacent year labels COLLIDE and render as mush ("231[]2356" where 2311 and 2356 overlap); the log scale bunches every dot into the right-hand half leaving a large dead gap after the TODO dot; visually it is a thin bare line with dots and reads unfinished; and with only a handful of dispatches it looks sparse. Note the date ceiling is now ~4000 years (deep band dropped), so the log scale matters less than it did and a linear or banded axis may now be viable. Options to weigh: collision-aware label placement (alternate above/below, or drop labels and show on hover); replace the continuous axis with era buckets ("next century / 2100s-2400s / beyond") as columns with counts, which also scales as the archive grows; merge it into the dispatch list below (currently redundant - the list already shows every date); or cut it. Charlie should pick from rendered options per the labelled-visual-options rule. Code: `_timeline()` and `_CSS` in `archive.py`.
  </details>
- [x] Custom domain LIVE and verified 06/08/2026: `https://aftertimes.charlietrenorden.com` returns 200 over HTTPS (cert provisioned, DNS on the GitHub Pages IPs, `CNAME` file committed), and `/assets/email-masthead.png` resolves - which matters because the newsletter builds its links and masthead URL from `site.base_url`.
- [x] **DONE 06/08/2026 - now sending from the authenticated domain.** `sender_email` is `aftertimes@mail.charlietrenorden.com`; the Brevo sender was created via API (id=3, no SPF/DKIM errors, auto-verified instantly as predicted) and **verified end to end**: campaign 38 went out as `The Aftertimes <aftertimes@mail.charlietrenorden.com>`, not `brevosend.com`.
  Built along the way: `brevo_sender.py` + a manual-only `brevo-admin` workflow (`list` / `campaign` / `create`) so sender admin runs where `BREVO_API_KEY` already lives instead of the key being copied to a laptop; `campaign` re-checks the actual From on the latest campaign.
  **Two gotchas hit that are NOT in the original note:** (1) the Brevo `/senders` endpoints sit behind Cloudflare bot rules that reject urllib's default agent with `403 error 1010 browser_signature_banned` - a real User-Agent header is required, and without it the domain lookup returns empty, which looks exactly like "the domain is not authenticated" (a false negative that briefly sent me the wrong way). `/emailCampaigns`, which `send_email.py` uses, is not blocked. (2) `test_send=true` used to run the WHOLE daily job, so testing the newsletter would burn a day of model quota and republish over the live page; the generate and commit steps are now skipped for a test send.
  Also added a `test_email` workflow input overriding the `BREVO_TEST_EMAIL` secret, per the One Story pattern.
  <details><summary>Original note (kept for the reasoning)</summary>
  **Problem:** `config/settings.yaml` has `newsletter.sender_email: charlie.rochfordgroup@gmail.com`. Brevo cannot authenticate a freemail address, so it rewrites the From to its own **shared** domain `<id>.brevosend.com`. SPF/DKIM/DMARC all pass - it is not an auth failure - but you inherit that shared domain's cold reputation, and the result is that a new subscriber's **first** edition reliably lands in spam. One Story had exactly this for weeks.
  **Why it is cheap:** `mail.charlietrenorden.com` is **already authenticated in Brevo** (done 06/08/2026 for One Story) in the **same Brevo account** and on the **same root domain** this site already uses (`aftertimes.charlietrenorden.com`). So the DNS work is done - do NOT add any DNS records, and do NOT touch the branded subdomain (see gotcha 2).
  **Steps:**
    1. Brevo -> Senders, Domains & IPs -> **Senders** -> *Add sender*: From Name `The Aftertimes`, From Email `aftertimes@mail.charlietrenorden.com`. It auto-verifies **instantly with no confirmation email**, because the domain is authenticated. Confirm the row shows *Verified* with DKIM signature `mail.charlietrenorden.com` and "DMARC is configured".
    2. Set `newsletter.sender_email: "aftertimes@mail.charlietrenorden.com"` in `config/settings.yaml` (leave `sender_name` and `list_id: 3` alone). Commit + push - the daily job sends from `main`.
    3. Test: `gh workflow run <daily workflow> -f test_send=true`, which sends to the `BREVO_TEST_EMAIL` secret. **Note:** unlike One Story, this repo's `workflow_dispatch` has no `test_email` input, so you cannot target an arbitrary address without adding one (One Story's `daily.yml` has the pattern to copy - a `test_email` string input that overrides the secret). Verify the received email's **From** reads `@mail.charlietrenorden.com`, NOT `brevosend.com`, and that it landed in the inbox.
  **Two gotchas that cost real time on One Story:**
    1. **An authenticated domain is NOT sufficient to send.** The address must ALSO exist as a Sender or the campaign API returns `400 invalid_parameter "Sender is invalid / inactive"`. Step 1 is not optional.
    2. **Do not opt into a branded subdomain.** Brevo refuses to mark a domain authenticated while the *optional* branding records mismatch, so it turns a nice-to-have into a hard blocker - and its record values are unreadable programmatically (truncated in the UI, absent from the accessibility tree, page JS blocked). Irrelevant if you leave it alone, which you should.
  **Expected result:** One Story's very first send from this authenticated domain **landed in the inbox with no warm-up period**, so expect the same rather than a slow reputation build.
  </details>
- [x] **DONE 12/08/2026 - the new cron is reliable.** Verified with `gh run list --workflow=daily.yml --json event,createdAt,conclusion`: ten consecutive `schedule` events across five days (07/08 to 11/08), both the ~20:13 and ~22:13 UTC slots every day, all `success`, and the 22:13 backup logs "already filed" as designed. No dropped runs since the fix. Nothing further to watch.
  <details><summary>Original note</summary>
  **Confirm the new cron actually fires (one glance, 07/08/2026 morning).** GitHub DROPPED the 06/08 scheduled run entirely - no run record, so nothing published and no email went out; it was recovered by a manual `workflow_dispatch` (campaign 40). Fix in `b137806`: primary cron moved off the congested top of the hour to `13 20 * * *`, plus a `13 22 * * *` backup in the SAME UTC day, guarded by `run.already_filed()` so the backup no-ops instead of republishing over an already-emailed edition. The guard is verified (5 tests + a real local run that correctly refused to refile); **the schedule itself cannot be tested on demand** - `workflow_dispatch` is a different event type. Check `gh run list --workflow=daily.yml --json event,createdAt`: expect a `schedule` event ~20:13 UTC, and a second one ~22:13 UTC whose log reads "already filed". After a few clean days this needs no further watching. See [[github-cron-unreliable]].
  </details>
- [ ] **PARTLY DONE - the harness shipped, the promote path and the marking did not.** Re-checked 12/08/2026:
  - **Built:** `trial.py` (slug-keyed, writes `data/trials/`, skips illustration, `--render` builds the mark-up page) and `mark.py` (harvests marked sentences into `config/funny_lines.yaml`). **Ten trials generated** and sitting in `data/trials/t001..t010.json`.
  - **NOT built:** `verdict.py` still has **zero** references to trials (`grep -c trial verdict.py` returns 0), so the trial mode described below was never added and a good trial cannot promote into `config/exemplars.yaml`.
  - **NOT done:** only **1 of the 11 trials has been marked** (re-counted 18/08/2026 - it was 1 of 10, a further trial has landed since), which is where the whole `funny_lines.yaml` pool of four lines came from. Ten trials are generated, paid for, and unread. Re-verified 18/08: `config/exemplars.yaml` still holds 0, `data/verdicts.json` still does not exist. This is the cheapest quality lever available and it needs Charlie for an hour.
  - **Consequence, measured 12/08/2026:** `config/exemplars.yaml` holds **0 exemplars** and `data/verdicts.json` does not exist. The few-shot pool the design calls the highest-leverage lever has never had anything in it.
  <details><summary>Original note</summary>
  **Batch-generate 10 trial dispatches for Charlie to verdict in one sitting.** Asked for 07/08/2026. The point is to bootstrap `config/exemplars.yaml` fast: one dispatch a day means the few-shot pool takes weeks to acquire any steer, and the pool moves generation harder than any prompt edit. **This is NOT ten runs of `run.py`** - that would collide on today's date, trip `already_filed()`, republish the live page ten times and pollute the ledger, bible and archive. It needs a trial harness:
  - Generate to `data/trials/<slug>.json`, keyed by slug not `run_date`. Touch **nothing** live: no `index.html`, no `archive.html`, no `d/`, no ledger, no bible, no `data/dispatches/`.
  - **Skip illustration** - the verdicts are about prose, and it saves 10 Cloudflare calls.
  - Vary the inputs the way real days do (dateline era, domain, style, place-kind, comic engine) or the sample tells you nothing about variance; feed the anti-repeat off the trial set itself, not the live ledger.
  - `verdict.py` needs a trial mode: `record()`/`pending()` are date-keyed and `_promote_if_good()` reads `data/dispatches/<date>.json`, so all three need to accept a trial path before a "good" can promote.
  - Render the 10 to a single scratch HTML page so Charlie reads them the way a reader would, not as JSON.
  - **Quota is the real constraint:** ~6 Gemini calls per dispatch (ideate + 3 drafts + judge + revise), so 10 trials is ~60 calls on a free tier that 429s under heavy same-day use - and the daily cron must still get through. Run the batch AFTER the day's dispatch has filed, and be ready to resume part-way rather than restart.
  </details>
- [ ] **BLOCKED ON CHARLIE - the Brevo account is under validation and no newsletter has gone out since 09/08/2026.** Every send returns `HTTP 402 {"code":"account_under_validation"}`. Both this project and One Story share one free Brevo account, so a single hold silenced both. Diagnosed 12/08/2026; Charlie parked it to resolve from the machine already logged in.
  - **The account is `charlie.rochfordgroup@gmail.com`** (company name "One Story", free plan, 300 credits). Recovered without a login via the new `brevo_sender.py whoami` action, because nobody knew which address it was. Brevo's validation request will be in THAT inbox, not `charlie.tren@gmail.com`.
  - **Nothing is misconfigured.** `mail.charlietrenorden.com` is `authenticated=True verified=True`, all three senders are active, and the API key still returns HTTP 200 on every read endpoint. The hold is account-level, on a human review queue.
  - **The list-bombing framing is WRONG - corrected 13/08/2026 by `brevo_sender.py diagnose`.** There are **7 contacts in the entire account**, newest 05/08, no burst, no junk domains, zero blacklisted on list 3. Nothing was bombed. Single opt-in may still be what a reviewer objects to in principle, but it is not the trigger the earlier note assumed, and the double-opt-in work should not be justified on that basis.
  - **FAR BIGGER FINDING: the newsletter has been delivering to NOBODY, since before the hold.** Campaigns target `list_id: 3`, which contains exactly **one** contact (`charlie.tren@gmail.com`), while the only recent organic signup (`zecchaeusnoller@gmail.com`, 05/08) sits in **list 2, "Your first list"** - Brevo's default. And campaign 46, fetched in full, reports `sent: 0, delivered: 0` - verified a second way, so it is a real zero, not an unpopulated field. Campaigns 39-46 (07-10/08) all report the same. **Every "successful" send in the logs reached zero inboxes.** The Brevo hold is therefore the second problem, not the first: fixing it would restore sending to an empty list.
  - **No backfill.** The 402 means the campaign was never created, so the 09/08, 10/08 and 11/08 editions will never be emailed once the hold lifts. They are in the archive; subscribers simply missed them.
- [x] **DONE then SUPERSEDED 17/08/2026.** Both faults were fixed - `send_email.py` latches `data/send_hold.json` on a 402 and short-circuits (still present, still tested), and `daily.yml` gained a step filing a GitHub issue on a failed send. Then the newsletter was retired the same week and the send step, the issue alarm and the `issues: write` permission all came out of the workflow; `daily.yml` now mentions email only in a comment. **The hold logic is live code wired to nothing** - keep it, it is the reversibility the retirement note promises. The transferable lesson outlived the feature and is in [[github-cron-unreliable]] and the repo `CLAUDE.md`: a step with `continue-on-error` makes a dead feature look identical to a healthy one, so it needs an alarm that is not the feature itself. Original entry: ~~Stop retrying the send while the account is under review, and make a send failure visible~~ - two separate faults, both live at the time:
  - Since 09/08 both projects have called campaign creation every day, twice daily on this one counting the backup cron. That is roughly a dozen rejected attempts against an account already under manual review, which reads as retry abuse to whoever is assessing it. Gate the send so a `402 account_under_validation` short-circuits subsequent attempts rather than hammering.
  - **The send step exits 1 but the run still reports `success`** in `gh run list` - the failure appears only as a job annotation. That is why a dead newsletter went unnoticed for three days. Fail the job properly, or better, have the send failure open an issue so it actually reaches Charlie. This is the more expensive of the two bugs: the 402 is Brevo's, the invisibility is ours.
- [x] **MOOT 17/08/2026 - there is no signup form.** Verified: `signup_form_url` is `""` and `newsletter.enabled` is false, so this site collects no addresses at all. Retiring the newsletter dissolved the list-provenance concern rather than fixing it. **If the newsletter is ever revived, this comes straight back** - and note the premise below is itself wrong: the account held only 7 contacts with no signup burst, so single opt-in was never the demonstrated trigger (the suspension was categorised Terms of Use, i.e. content). Original entry: ~~Switch both signup forms to double opt-in.~~ Directly answers the list-provenance concern that most likely caused the hold, and it is a change in Brevo's form settings, not in this repo. One code change follows: `render.py`'s `_signup()` success copy currently promises "Thanks - your first dispatch arrives tomorrow morning", which becomes a lie under double opt-in and must read "check your inbox to confirm".
- [ ] **`run_date` is computed in UTC, not Sydney.** `datetime.now(timezone.utc).date()` at `run.py:174` and `run.py:331`, despite `timezone: Australia/Sydney` in settings and a `common.tz_now()` helper existing for exactly this. The cron fires 20:13 UTC, so the edition that reaches a Sydney reader on the morning of the 12th is filed as the 11th - the JSON key, the `d/<date>.html` permalink and the `assets/img/<date>.jpg` name are all a day behind the masthead, which is itself correct because it formats `run_time` in Sydney. No reader sees it, but it is why the newest archive row looks a day stale. **Not a free fix:** changing it renames permalinks and shifts the `already_filed()` key, so it needs a decision on whether to migrate the existing `d/` files or accept a one-day seam.
- [x] **DONE 26/08/2026 - `proposals.py` is wired to the Monday run.** Charlie asked for the call either way; wiring won on cost, because `proposals.build` is a pure function over records already in memory - no API call, no new failure mode on the publish path. It proposes and never applies, which is the design: an unattended process editing the house voice is the highest-blast-radius change available. Its first run already found a rule breach the prompt explicitly forbids - "Kaelen Voss" in three dispatches against a ban on repeated character names - plus "is no longer" in three. `docs/proposals.md` was also added to `daily.yml`'s `git add`, without which the document would be written every Monday and never committed: the same written-but-unwired failure in a new costume. Five tests, including one asserting the workflow commits it.
- [ ] **Check the first live dispatches from the new image pipeline (17/08/2026 onward).** `depict.py` shipped today and tonight's cron is its first unsupervised run. Two residuals seen in testing that need a verdict across a few real dispatches rather than one A/B: **giant foreground objects** (forks rendered at absurd scale in three of four test renders - flux exaggerating foreground perspective, and `no object is impossibly large` in the brief prompt did not stop it), and **the sky coming out as smooth grey wash rather than cross-hatching** despite the negative now demanding engraved tone. If either recurs on most dispatches it is systematic and worth prompt work; if it is one in five, leave it. Compare against `data/trials/img/style-A-dore.jpg`, which is the agreed look.
- [ ] **Nine trial dispatches are unmarked.** `t001`, `t003`-`t011` in `data/trials/` have had no funny-line verdicts; only `t002` was marked, and all four lines currently steering the write prompt come from that ONE dispatch. Until the pool draws on several sources it is steering toward one paragraph's texture rather than Charlie's taste in general - and a lopsided pool is exactly how "why is it always unpaid debts" happened. `python trial.py --render` rebuilds the numbered mark-up page; `python mark.py <slug> <numbers>` records them.
- [ ] **Verdict backlog: 8 dispatches await a good/bad call** (2026-07-29 through 2026-08-05). Charlie parked this on 06/08/2026. `python verdict.py` lists them; `python verdict.py <date> good|bad "why"` records one. This is the ONLY input the learning loop cannot compute for itself, and a "good" promotes that premise into `config/exemplars.yaml`, which steers every future generation harder than any prompt instruction. He has already called 2026-07-30 (the council-complaint one) and 2026-08-03 (the whale liver) bad in conversation, so those two are safe to record on his say-so. NOTE: `config/exemplars.yaml` is NOT in the daily workflow's `git add` list (it commits `index.html archive.html d/ data/ assets/img/`), which is correct because verdicts are recorded locally - but it does mean the pool must be committed by hand after recording verdicts.
- [x] **SUPERSEDED 18/08/2026 - the FUTURES VISITED bar is gone.** Charlie cut it rather than have it restyled: "also remove this". The right call. It had been reworked twice (sqrt scale on 06/08, four-tier collision avoidance on 12/08) and each pass fixed a measurable defect without ever making it look like anything - it stayed a hairline rule with dots, and the tiering that stopped the labels colliding is what made it read ragged. It also duplicated the year rail the list underneath already carries, so every date appeared twice on one page. `_timeline()`, its three constants, the whole `.tmark`/`.tyear`/`.taxis` CSS block and the timeline half of the filter JS are all removed; the filter now has one set of items to hide instead of two. **Do not rebuild it** - a `test_the_futures_visited_bar_is_gone` guard fails if the markup comes back.
- [x] **DONE 18/08/2026 - the archive masthead now matches the front page.** Charlie: "the aftertimes logo on the browse the archive page needs to be the same as on the main page". It was plain bold Georgia at `clamp(2rem,8vw,3rem)` while the front page wore the blackletter flag at `clamp(2.7rem,10vw,4.6rem)`, and `archive.py` never served the `@font-face` at all, so the family name silently fell through to serif. Verified in a browser with the font actually loaded: every masthead property - family, size, weight, line-height, letter-spacing, tag size and spacing, the double rule - now compares identical between the two pages. The values are duplicated rather than shared, because render.py builds its font path per page depth; both sides carry a comment saying to change them together.
- [ ] Optional self-test: subscribe with your own email on the live site and confirm the contact lands in the Brevo "The Aftertimes" list (#3).
- [ ] **Code tidy, all verified present 06/08/2026, all deferred from the multi-draft-pipeline code review as minor.**
  - `critic.py:121` hardcodes the present-day-prop escalation boundary (`>= 400` years), duplicating a number that also lives in `write._era_rule` and in `settings.yaml` `dates.bands.near`. The spec's stated principle is that every threshold is tunable without editing code, so it belongs in the `quality` block. `_STATED_JOKE_SENTENCES = 2` is hardcoded for the same reason.
  - `_LEGAL` is now compiled in THREE places with three different word lists: `critic.py` (production), `tests/test_registers.py` (seed-balance guard) and `exemplars.py` (pool guard). They will drift. Pick one owner and import from it, or document deliberately why each differs.
  - `critic.py:137` `_US_WORDS` rebuilds the exact alternation `write._au_spelling` already builds; export one helper from `write.py`.
- [ ] **`Other Projects` backlink is only on The Aftertimes.** Verified 06/08/2026: neither `one-story` nor `macro-signals-web` (Crowdwise) links back to the hub, and DCF Studio was not checked. If the cross-linking is wanted for consistency, add the same footer link there. Charlie has not asked for this - it is my observation, so confirm before doing it.
- [ ] **Hub thumbnail for The Aftertimes is stale.** The screenshot on charlie-tren.github.io predates the locator chart, the reworked metadata panel and the custom domain, so the card no longer looks like the site. Re-shoot with headless Chrome (the original pattern is in the hub repo).

## Done
- [x] **Deployed + live** at https://the-aftertimes.github.io/ (org `the-aftertimes`, Pages from main root, daily Actions cron 20:13 UTC + a 22:13 UTC backup, `.nojekyll`).
- [x] **Daily newsletter LIVE** - subscribe box (Brevo form -> list #3, single opt-in) + `send_email.py` daily send (per-day guard, `test_send` toggle); `BREVO_API_KEY` + `BREVO_TEST_EMAIL` secrets set and a test verified to both inboxes. Cron sends live each morning.
- [x] **Linked from the personal hub** (charlie-tren.github.io) with a screenshot thumbnail.
- [x] **Pictures** - Cloudflare Workers AI engravings (greyscale, story-scene-derived, caption cropped, graceful fallback).
- [x] **Look** - tagline "Tomorrow's headlines, a little early"; NYT-blackletter masthead; edition line; actual-date dateline; Title-Case domain; glossary removed from the page.
- [x] **Email polished** - blackletter-image masthead, correct tagline, embedded engraving, single-tone background, bottom padding.
- [x] **Writing tuned** - comedic-engine premises (best-first), kicker + no forced metaphors, banned recurring names, 11 distinct style shapes (only court/notice are "ruling"-shaped), permanent idiom-slip fixer.
- [x] Secrets set: `GEMINI_API_KEY`, `CF_ACCOUNT_ID`, `CF_API_TOKEN`, `BREVO_API_KEY`, `BREVO_TEST_EMAIL`.
- [x] Launch-day duplicate edition self-healed (cron regenerates fresh, non-duplicate editions via the ledger).
- [x] **Pro write-model + flash fallback** (30/07/2026), then **DISABLED 31/07/2026**. The fallback machinery works and is retained, but `write_model` is now blank: **every Pro-tier model (`gemini-3.1-pro-preview`, `gemini-3-pro-preview`, `gemini-pro-latest`, `gemini-2.5-pro`) returns HTTP 429 on this free key - the free tier has ZERO Pro quota, exactly like image generation.** It never once served (every dispatch to date was flash) and leaving it on burned a wasted call + ~1.5s each run. **Do not re-enable without a paid key.** Prose quality therefore rests entirely on the prompts.
- [x] **Locator chart** (30/07/2026). Per-dispatch ink-on-bone SVG (circular celestial plate): centre = now, marker = the story's dateline, radius log-scaled by years-from-now. Deterministic (sha256(scrubbed place + year) -> splitmix64), so index/permalink/replay match byte-for-byte. Inline SVG, web only (not email). Charlie picked the plate from 3 labelled options.
- [x] **Archive domain filter** (30/07/2026). Chips built from the observed domain set (deduped on a normalised key), filtering both the list and the future-timeline together; defaults to all-visible so it works with JS off.

## Free-model survey (04/08/2026) - read this before shopping for a better model

Gemini free tier is **flash only** (every Pro model 429s - zero Pro quota).
So the other free option already wired to this project is **Cloudflare Workers AI**,
on the SAME `CF_ACCOUNT_ID` / `CF_API_TOKEN` already used for the engravings.
It exposes **26 text-generation models**. Tested on an identical write prompt:

| Model | Result |
|---|---|
| `@cf/openai/gpt-oss-120b` | **The only real contender.** 3/3 reliable, but ONLY with `max_tokens>=4000` - it is a reasoning model and burns 3k-9k chars of thinking first; at 2000 it returns null content. Metrics were fine (225w, mean 15.0, longest 23). Prose was competent but noticeably LESS FUNNY than flash - more worldbuilding, weaker jokes. Emits U+2011 non-breaking hyphens (which is how the `hyphenate()` gap was found). |
| `@cf/nvidia/nemotron-3-120b-a12b` | Empty response - reasoning consumed the budget. |
| `@cf/meta/llama-3.3-70b-instruct-fp8-fast` | Catastrophically over-applies the rhythm rule: mean sentence 5.0 words, 17 short sentences, 104 words total - reads like a children's book. Also **lifted a prompt example verbatim** into the story. |
| `@cf/mistralai/mistral-small-3.1-24b-instruct` | Returned no usable JSON. |
| `@cf/moonshotai/kimi-k2.6`, `@cf/zai-org/glm-5.2` | HTTP 403 - not available on this plan. |

**Conclusion: stay on gemini-3.6-flash.** It is currently the funniest of the free
options and the most reliable at returning clean JSON. `gpt-oss-120b` is the
documented fallback if flash quota ever disappears - all CF models are
OpenAI-shaped (`result.choices[0].message.content`), unlike Gemini.
Untested elsewhere (would need a new key): Groq, GitHub Models, OpenRouter free tier.

## An imageless dispatch is never retried (27/08/2026)

Today's dispatch published with `image: none (fallback)` because Cloudflare's free
allocation was exhausted, and there is **no path that ever gives it a picture**.
The dispatch is filed, `already_filed()` correctly refuses to refile it, and
`reillustrate` has to be run by hand - I ran it and it failed the same way, twice.
The allocation resets at 00:00 UTC / 10:00 AEST.

Two facts that make this sharper than "the quota ran out":

- **The daily jobs run at the most depleted point of the UTC quota day.** Publishing
  at 06:00-07:00 AEST means 20+ hours into the UTC day. Moving the primary to 19:13
  UTC helps slightly; it does not fix the shape.
- **The allocation is shared with Photocopy** per illustrate.py's own error text -
  but on 27/08 Photocopy drew successfully at 22:06 UTC while this repo was refused
  at 21:42 and again at 22:09. So either they are on different accounts and that
  note is wrong, or the models have separate limits. **Worth establishing which
  before designing around it** - the fix differs completely.

- [ ] **Retry the picture once the UTC day rolls.** A cron just after 00:00 UTC that
  runs reillustrate ONLY when the current dispatch has no image. `reillustrate.py`
  already does the work and takes a date, but it redraws unconditionally, so it
  needs a guard - otherwise a nightly retry quietly replaces good art and burns the
  next day's allocation. Lands at 10:00 AEST, after Charlie reads, which is worse
  than in-time and much better than never.
- [ ] **Establish whether the Cloudflare allocation is genuinely shared with
  Photocopy.** `cf-usage` returns 400 on `/accounts/*/ai/usage`, so it cannot
  currently answer this. Fix the probe first; the answer decides whether the two
  projects need to coordinate or simply have separate budgets.

## Later (v3 ideas)
- [x] **Raster favicons still carry the old bone-paper hex** (26/08/2026). DONE same day - `tools/make_icons.py` now renders all four from `assets/favicon.svg`, so the tile colour and the glyph colour have one source of truth and the next palette change is one command. Preserves the two conventions the hand-made files already had: transparent corners on the tab icons, opaque full-bleed on apple-touch (iOS paints transparency black). Not wired into CI on purpose - the SVG draws its "A" with `font-family="Georgia"`, so a Linux runner would substitute a different serif.
- [ ] **Style-transfer the locator into an engraving.** The chart is clean vector; a future pass could render it as a hand-engraved star plate to sit even closer to the Dore illustrations.

- [x] ~~**The quality judge is saturated, so best-of-3 is picking at random**~~ -
      **WRONG, WITHDRAWN 29/08/2026 the same day it was written.** Both halves of it were
      mistaken and the corrections are worth keeping, because the mistakes are easy to
      make again from the same data.

      **1. The 1.0s are not a saturated quality score.** `critic.score` returns
      `1.0 - penalty` where the penalty comes from RULE VIOLATIONS - length, rhythm,
      plainness, register, props, residue. A 1.0 means "broke no rules", so 37 of 65
      drafts scoring 1.0 is a compliance floor working correctly, not a judge that
      cannot discriminate. Comedy is judged somewhere else entirely, by `judge.py`,
      qualitatively, in its own call. I read the distribution and diagnosed the wrong
      instrument.

      **2. `judge_pick` and `chosen_index` are not the same index space,** so the
      "selection follows neither signal" measurement was comparing two different
      orderings. `run.py` shows the judge the `pool`, which is the survivors re-sorted
      by score, and records `judge_pick` against THAT; `_finish` then records
      `chosen_index` against the original `drafts` order, deliberately, so the learning
      loop can find the published draft later. Reconstructing the pool and translating
      between them, they agree on **3 of 3** - every dispatch that has both a judge call
      and a kept `drafts` field, which only exists from 26/08. Small n, and it is all
      the data there is.

      The 29/08 case that looked like an off-by-one is the same thing: the judge returned
      `pick: 1` and its reason described corporate title inflation, which is draft index
      1 in the original order but the FIRST draft in the pool it was shown. It was right.

      **What actually stands:** the drafts are genuinely diverse - 29/08 produced three
      unrelated stories, not three versions of one - so "the drafts are too alike" is
      also ruled out. Charlie's sense that the output is not great is still unexplained
      by anything in the metrics, which means the next move is to READ a week of
      published dispatches rather than to measure anything further. Logged as its own
      item below.

- [ ] **Check the cast actually diversifies** (fix shipped 29/08/2026, commit 2b20794,
      EFFECT UNVERIFIED). `trends.repeated_names` now counts surnames as well as full
      names, so the avoid block went from one entry to four - Chen x6, Kaelen Voss x3,
      Osei x3, Thorne x3. That is the block the writer RECEIVES; whether it changes what
      the writer DOES is a separate question, and prompts do not always obey.
      **The measurement, in a week or so:** re-run the count over the newest 10 dispatches
      and compare against today's baseline of 17-of-31 using one of those four surnames.
      A flat or rising rate means the avoid block is being read and ignored, which is a
      prompt-placement problem rather than a detection one - and the Ghostwriters fix the
      same week was exactly that, moving the voice to the END of the prompt where the
      model reads it last.
      One line to get the number:
      `python -c "import sys,glob,os;sys.path.insert(0,'.');import trends,avoid;from common import read_json,load_yaml;r=[read_json(f'data/dispatches/{os.path.basename(f)}') for f in sorted(glob.glob('data/dispatches/*.json'))];print(trends.detect(avoid.recent([x for x in r if x],10),3))"`
